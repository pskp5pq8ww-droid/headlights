# Route all requests through the static/PHP public folder.
# Used when Hostinger's document root cannot be changed directly to /public.
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ /public/$1 [NC,L,QSA]
</IfModule>
