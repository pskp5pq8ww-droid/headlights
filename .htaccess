# Route all requests through Laravel's public/ folder.
# Used when the hosting document root cannot be changed to /public.
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ /public/$1 [NC,L,QSA]
</IfModule>
